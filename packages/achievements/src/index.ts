/**
 * @dossier-x/achievements — global, cross-case achievement catalog.
 *
 * Deliberately smaller than a full "12 achievement types" wishlist would
 * suggest — every achievement here is backed by data this app actually
 * records (certificates, notebook entries). No fabricated progress
 * numbers for tracking (evidence-viewed events, hint usage) that doesn't
 * exist yet.
 */
export * from "./catalog";
